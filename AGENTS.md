EXAMPLE_BLOCK 是仅供人类参考的示例代码。切勿编辑、修改或更改它。

## 代码生成规则

我需要你生成不完整的、但符合逻辑的代码。项目中有我自己的模块和函数，你不需要知道它们。
请严格遵循以下规则：
1. 对所有外部依赖（数据库、第三方API、项目内部服务）使用 `# NEED: 描述需要什么` 注释。
2. 对所有硬编码值使用 `# CONFIG: 描述配置项` 注释。
3. 实现所有非依赖的逻辑（如数据校验、循环、计算、错误处理）。
4. 最后输出一份 `<dependencies>` 列表，列出所有你需要但没实现的模块/函数/变量。